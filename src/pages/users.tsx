import { jsx } from '@theme-ui/core';
import { Box } from '../../lib';
const Page = () => {
  return <Box></Box>;
};
export default Page;
export const getServerSideProps = async () => {
  return { props: {} };
};
